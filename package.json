{
  "name": "ai-revenue-recovery-combined",
  "private": true,
  "version": "1.0.0",
  "description": "Combined single-process deployment: NestJS backend (under /api) + Next.js frontend (everything else), for hosts with a limited number of app slots.",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "node server.js"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  }
}
